import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keep this minimal until we know what we need.
  // Add image domains, rewrites, etc., as the app evolves.
};

export default nextConfig;
